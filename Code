
a = float(input("smaller:"))
b = float(input('bigger:'))
ca = float(input('increase of smaller:'))
cb = float(input('increase of bigger:'))
y = 0

while a < b:
    a = a + a*ca
    b = b + b*cb
    y += 1
print('In',y,'units of time for a > b')
